export interface Group {
  id: string
  name: string
  createAt: string
  updateAt: string
}

export interface Expense {
  id: string
  amount: string
  description: string
  createAt: string
  updateAt: string
}